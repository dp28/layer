#! /usr/bin/env ruby

require '/usr/local/lib/layer/layer'

begin
  Layer::Layer.new(ARGV).update_background
rescue OptionParser::InvalidOption => err
  abort "layer: #{err.message}\nFor full usage information use layer -h"
end