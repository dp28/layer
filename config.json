{
  "allow-scroll": {
    "default": false,
    "describe": "Allow the terminal image to scroll when the foreground scrolls"
  },
  "template": {
    "default": "layer-cheatsheet",
    "describe": "The saved template to render",
    "alias": "t"
  },
  "template-jade": {
    "describe": "The jade of the saved template to render",
    "alias": "j"
  },
  "template-data": {
    "describe": "The data to use when rendering the template",
    "alias": "d"
  },
  "row-height": {
    "default": 18,
    "describe": "The height of a terminal row in pixels"
  },
  "column-width": {
    "default": 9,
    "describe": "The width of a terminal column in pixels"
  },
  "profile": {
    "default": "Default",
    "describe": "The gnome terminal profile to set the background for"
  },
  "image-file": {
    "default": null,
    "describe": "Where to save the background image"
  }
}